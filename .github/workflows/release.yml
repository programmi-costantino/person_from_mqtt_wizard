name: Create Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: write

jobs:
  release:
    name: Build ZIP and create GitHub Release
    # MODIFICATO: puntiamo al sistema autoscale
    runs-on: "tier-mid"
    steps:
      - uses: actions/checkout@v6

      - name: Get version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create release ZIP
        run: |
          # Installiamo zip se non presente (nei container è spesso assente)
          sudo apt-get update && sudo apt-get install -y zip
          cd custom_components/person_tracker_from_mqtt/
          zip ../../person_tracker_from_mqtt.zip -r ./

      - name: Generate changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@v6
        with:
          configuration: |
            {
              "categories": [
                { "title": "## ✨ New features", "labels": ["enhancement", "feature"] },
                { "title": "## 🐛 Bug fixes", "labels": ["bug", "fix"] },
                { "title": "## 📚 Documentation", "labels": ["documentation"] },
                { "title": "## 🔧 Other", "labels": [] }
              ],
              "template": "#{{CHANGELOG}}\n\n**Full changelog**: #{{RELEASE_DIFF}}"
            }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: person_tracker_from_mqtt.zip
          body: |
            ## Installation via HACS
            Install or update via HACS — the new version will be offered automatically.

            ## Manual installation
            Download `simple_pid_controller.zip`, extract and copy `simple_pid_controller/` to your `custom_components/` directory.

            ${{ steps.changelog.outputs.changelog }}
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
